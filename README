lumeter showes speeds information gathered from various interfaces from different machines (running lumeterd)
to start type ./lumeter [ifname=eth0]
if ifname is empty, buil-in xAP-server will be send it's information on eth0
on remote machines please start lumeterd from ./console/builds/$(PLATFORM)/ directory.

